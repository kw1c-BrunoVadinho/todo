{% extends "task/index.html.twig" %}

{% block title %}Login {% endblock %}
{% block content %}
    {% if message is defined %}
        <p class="text-center mb-2">
            {{ message }}
        </p>
        <div class="flex justify-center">
            <a href="{{ back }}" class="px-5 py-1.5 bg-blue-400 rounded-2xl border-2">BACK</a>
        </div>
    {% endif %}
{% endblock %}